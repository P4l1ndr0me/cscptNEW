package entities;

import static com.raylib.Raylib.*;

public class Player extends Entity {
    public Player(Vector2 position, float scale, float speed, Texture texture) {
        super(position, scale, speed, texture);
    }
}
